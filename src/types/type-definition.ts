export type User = {
    name: string;
    email: string;
    password: string;
    favorite: number[];
}

export type inform = {
    id: number;
    name: string;
    type: string;
    group: string;
}